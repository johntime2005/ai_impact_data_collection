# Analysis 目录

本目录用于存放可执行的数据分析脚本，这些脚本可以自动化运行并生成分析结果。

## 📁 目录结构

```
analysis/
├── exploratory/      # 探索性分析脚本
├── statistical/      # 统计分析脚本
├── visualization/    # 可视化脚本
└── reports/          # 报告生成脚本
```

## 🎯 各子目录用途

### exploratory/ - 探索性分析
用于数据的初步探索和剖析：
- `basic_stats.py` - 基础统计信息（均值、中位数、分位数等）
- `data_profiling.py` - 数据画像（分布、缺失值、异常值）

**示例使用场景**：
- 快速了解数据集的基本特征
- 生成数据质量报告
- 识别需要进一步分析的数据模式

### statistical/ - 统计分析
用于深入的统计分析：
- `correlation_analysis.py` - 相关性分析
- `hypothesis_testing.py` - 假设检验

**示例使用场景**：
- 验证变量之间的关系
- 测试假设的显著性
- A/B 测试分析

### visualization/ - 可视化
用于生成图表和可视化：
- `plot_trends.py` - 趋势图
- `generate_charts.py` - 各类图表

**示例使用场景**：
- 自动生成定期报告的图表
- 创建仪表板数据
- 导出演示文稿用的可视化

### reports/ - 报告生成
用于生成完整的分析报告：
- `generate_summary_report.py` - 汇总报告
- `export_insights.py` - 导出分析洞察

**示例使用场景**：
- 定期生成周报/月报
- 导出结果给非技术人员
- 归档分析结果

## 💡 使用指南

### 1. 脚本应该是可执行的

每个脚本应该能够独立运行：

```python
# analysis/exploratory/basic_stats.py
import sys
sys.path.append('../..')  # 添加项目根目录到路径

from analytics.data_cleaner import DataCleaner
import pandas as pd

def main():
    # 加载数据
    df = pd.read_json('../../data/raw/merged_posts.json')

    # 进行分析
    stats = df.describe()

    # 保存结果
    stats.to_csv('../../outputs/results/basic_stats.csv')
    print("✅ 统计分析完成")

if __name__ == "__main__":
    main()
```

### 2. 使用 analytics 模块

尽量复用 `analytics/` 目录中的工具类：

```python
from analytics.text_analyzer import TextAnalyzer
from analytics.quality_analyzer import QualityAnalyzer
from analytics.report_generator import ReportGenerator
```

### 3. 保存输出到 outputs 目录

```python
# 图表保存到
outputs/figures/

# 报告保存到
outputs/reports/

# 数据结果保存到
outputs/results/
```

## 🚀 运行示例

```bash
# 运行探索性分析
python analysis/exploratory/basic_stats.py

# 运行可视化脚本
python analysis/visualization/plot_trends.py

# 生成报告
python analysis/reports/generate_summary_report.py
```

## 📊 与 Notebooks 的区别

| 特性 | Analysis 脚本 | Notebooks |
|------|-------------|-----------|
| **用途** | 自动化、生产级分析 | 交互式探索 |
| **运行方式** | 命令行执行 | Jupyter 环境 |
| **版本控制** | 友好（纯代码） | 较差（包含输出） |
| **可重复性** | 高 | 中等 |
| **调试** | 需要工具 | 实时交互 |

## 🔧 最佳实践

1. **模块化**: 将公共功能提取到 `analytics/` 模块
2. **参数化**: 使用命令行参数或配置文件
3. **日志记录**: 使用 logging 模块记录执行过程
4. **错误处理**: 妥善处理异常情况
5. **文档注释**: 清晰说明脚本用途和参数

## 📂 相关目录

- `../analytics/` - 可复用的分析模块（工具库）
- `../notebooks/` - Jupyter Notebooks（交互式探索）
- `../data/` - 数据文件
- `../outputs/` - 分析结果输出
